/** Single switch for the original and native-resolution render modes. */
export const RENDER_SCALE: number = 3 / 4;

export const SPRITE_SHEET_SCALE = 3 / 4;

export const LOGICAL_WIDTH = RENDER_SCALE === 1 ? 240 : 176;
export const LOGICAL_HEIGHT = RENDER_SCALE === 1 ? 294 : 220;
